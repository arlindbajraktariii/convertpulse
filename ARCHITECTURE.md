# ConvertPulse Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CONVERTPULSE                             │
│                  Landing Page Analytics SaaS                     │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   End User   │────────▶│  Client Page │────────▶│   Tracker    │
│  (Visitor)   │         │ (Landing Pg) │         │   Snippet    │
└──────────────┘         └──────────────┘         └──────┬───────┘
                                                          │
                                                          │ POST /api/events
                                                          │
                         ┌────────────────────────────────▼────┐
                         │         BACKEND API                 │
                         │     (Express.js + MongoDB)          │
                         │                                     │
                         │  ┌──────────────────────────────┐  │
                         │  │   Authentication Layer        │  │
                         │  │   (JWT + bcrypt)              │  │
                         │  └──────────────────────────────┘  │
                         │                                     │
                         │  ┌──────────────────────────────┐  │
                         │  │   API Routes                  │  │
                         │  │   • /auth  (login/register)   │  │
                         │  │   • /sites (CRUD sites)       │  │
                         │  │   • /events (tracking)        │  │
                         │  │   • /stats (analytics)        │  │
                         │  └──────────────────────────────┘  │
                         │                                     │
                         │  ┌──────────────────────────────┐  │
                         │  │   Database Models             │  │
                         │  │   • User                      │  │
                         │  │   • Site                      │  │
                         │  │   • Event                     │  │
                         │  └──────────────────────────────┘  │
                         └─────────────┬───────────────────────┘
                                       │
                                       │ MongoDB
                                       ▼
                         ┌─────────────────────────┐
                         │    MongoDB Database     │
                         │  ┌──────────────────┐   │
                         │  │ users collection  │   │
                         │  └──────────────────┘   │
                         │  ┌──────────────────┐   │
                         │  │ sites collection  │   │
                         │  └──────────────────┘   │
                         │  ┌──────────────────┐   │
                         │  │ events collection │   │
                         │  └──────────────────┘   │
                         └─────────────────────────┘
                                       ▲
                                       │ API Calls
                                       │
                         ┌─────────────┴───────────────────────┐
                         │      FRONTEND DASHBOARD              │
                         │      (Next.js + React)               │
                         │                                      │
                         │  ┌────────────────────────────────┐ │
                         │  │  Landing Page                   │ │
                         │  │  (Marketing + Pricing)          │ │
                         │  └────────────────────────────────┘ │
                         │                                      │
                         │  ┌────────────────────────────────┐ │
                         │  │  Authentication Pages           │ │
                         │  │  • Login                        │ │
                         │  │  • Register                     │ │
                         │  └────────────────────────────────┘ │
                         │                                      │
                         │  ┌────────────────────────────────┐ │
                         │  │  Dashboard                      │ │
                         │  │  • Analytics Charts             │ │
                         │  │  • Site Management              │ │
                         │  │  • Stats Overview               │ │
                         │  └────────────────────────────────┘ │
                         └──────────────────────────────────────┘
                                       ▲
                                       │
                         ┌─────────────┴───────────────┐
                         │      SaaS User              │
                         │   (Dashboard User)          │
                         └─────────────────────────────┘
```

## Data Flow

### 1. User Registration Flow
```
User → Frontend → POST /api/auth/register → Backend
                                          ↓
                                    Hash Password
                                          ↓
                                    Save to MongoDB
                                          ↓
                                    Generate JWT
                                          ↓
Frontend ← JWT Token + User Data ← Backend
     ↓
Store Token in localStorage
     ↓
Redirect to Dashboard
```

### 2. Event Tracking Flow
```
Visitor on Client Page → Tracker Snippet Loaded
           ↓
    Track Interactions:
    • Scroll (depth %)
    • Clicks (elements)
    • Time (seconds)
    • Pageview
           ↓
    Batch Events (every 5s or 10 events)
           ↓
    POST /api/events/batch
           ↓
Backend API → Verify API Key
           ↓
    Save Events to MongoDB
           ↓
    Return Success
```

### 3. Analytics Viewing Flow
```
User Opens Dashboard
     ↓
Select Site + Timeframe
     ↓
GET /api/stats/:siteId?timeframe=7d
     ↓
Backend → Verify JWT
     ↓
Query MongoDB Events
     ↓
Aggregate Statistics:
• Count pageviews
• Calculate avg scroll depth
• Count unique sessions
• Group top clicks
• Calculate daily breakdown
     ↓
Return Stats JSON
     ↓
Frontend ← Stats Data
     ↓
Render Charts (Recharts)
• Line chart (daily)
• Pie chart (scroll dist)
• Bar chart (top clicks)
• Stat cards (summary)
```

## Technology Stack Layers

```
┌─────────────────────────────────────────────┐
│           DEPLOYMENT LAYER                   │
│  Vercel (Frontend) + Render (Backend)        │
│  MongoDB Atlas (Database)                    │
└─────────────────────────────────────────────┘
                    ▲
                    │
┌─────────────────────────────────────────────┐
│           APPLICATION LAYER                  │
│  Next.js + React + TailwindCSS               │
│  Express.js + Node.js                        │
│  Tracking Snippet (Vanilla JS)               │
└─────────────────────────────────────────────┘
                    ▲
                    │
┌─────────────────────────────────────────────┐
│           DATA LAYER                         │
│  MongoDB + Mongoose ODM                      │
│  JWT Authentication                          │
│  RESTful API                                 │
└─────────────────────────────────────────────┘
```

## Component Breakdown

### Frontend Components
```
frontend/
├── Pages
│   ├── Landing (/)          → Marketing site
│   ├── Login (/login)       → User authentication
│   ├── Register (/register) → User signup
│   └── Dashboard (/dashboard) → Analytics view
│
├── Utilities
│   ├── API Client (axios)   → HTTP requests
│   └── Types (TypeScript)   → Data interfaces
│
└── Styling
    └── TailwindCSS          → UI styling
```

### Backend Components
```
backend/
├── Server (Express)
│   ├── CORS Middleware     → Cross-origin requests
│   ├── JSON Parser         → Parse request bodies
│   └── Route Handlers      → API endpoints
│
├── Authentication
│   ├── JWT Middleware      → Verify tokens
│   └── Password Hashing    → bcrypt
│
├── Models (Mongoose)
│   ├── User Schema         → User data structure
│   ├── Site Schema         → Site data structure
│   └── Event Schema        → Event data structure
│
└── Routes
    ├── /auth              → Login/Register
    ├── /sites             → Site CRUD
    ├── /events            → Event tracking
    └── /stats             → Analytics queries
```

### Tracker Components
```
tracker/
├── convertpulse.js
│   ├── Init Function       → Setup tracking
│   ├── Scroll Tracker      → Monitor scroll depth
│   ├── Click Tracker       → Capture click events
│   ├── Time Tracker        → Measure engagement
│   ├── Batch Queue         → Store events locally
│   └── Send Function       → POST to backend
│
└── Demo Page
    └── demo.html           → Test environment
```

## Security Architecture

```
┌────────────────────────────────────┐
│         Security Layers             │
├────────────────────────────────────┤
│ 1. HTTPS/SSL                       │ ← Transport Security
├────────────────────────────────────┤
│ 2. CORS Configuration              │ ← Origin Control
├────────────────────────────────────┤
│ 3. JWT Authentication              │ ← User Identity
├────────────────────────────────────┤
│ 4. API Key Validation              │ ← Site Verification
├────────────────────────────────────┤
│ 5. Password Hashing (bcrypt)       │ ← Data Protection
├────────────────────────────────────┤
│ 6. Input Validation                │ ← Prevent Injection
└────────────────────────────────────┘
```

## Scalability Considerations

### Current Architecture (MVP)
- Single backend server
- MongoDB with indexes
- Batch event processing
- Stateless API (JWT)

### Future Scaling Options
```
┌─────────────────────────────────────────┐
│  Load Balancer                          │
└─────────┬───────────────────────────────┘
          │
     ┌────┴────┐
     │         │
┌────▼───┐ ┌──▼────┐
│Backend 1│ │Backend 2│  → Multiple instances
└────┬───┘ └──┬────┘
     │        │
     └───┬────┘
         │
    ┌────▼────┐
    │ MongoDB │
    │ Cluster │  → Replica set
    └─────────┘
```

## API Request Flow

```
Client Request
     ↓
┌─────────────────┐
│ CORS Check      │
└────────┬────────┘
         │ ✓
┌─────────▼────────┐
│ Parse JSON Body  │
└────────┬─────────┘
         │
┌─────────▼────────────┐
│ Auth Middleware      │
│ (if protected route) │
└────────┬─────────────┘
         │ ✓ JWT Valid
┌─────────▼─────────┐
│ Route Handler     │
│ • Validate Input  │
│ • Query Database  │
│ • Process Logic   │
└────────┬──────────┘
         │
┌─────────▼──────────┐
│ Send JSON Response │
└────────────────────┘
```

## Deployment Architecture

### Production Setup
```
┌─────────────────────────────────────────┐
│           USER'S BROWSER                 │
└──────┬────────────────────────┬──────────┘
       │                        │
       │ HTTPS                  │ HTTPS
       │                        │
┌──────▼────────┐      ┌───────▼──────────┐
│   Vercel      │      │  Client's Site   │
│  (Frontend)   │      │  (with tracker)  │
└──────┬────────┘      └───────┬──────────┘
       │                       │
       │ API Calls             │ POST Events
       │                       │
       └───────────┬───────────┘
                   │ HTTPS
            ┌──────▼───────┐
            │   Render     │
            │  (Backend)   │
            └──────┬───────┘
                   │
                   │ MongoDB URI
            ┌──────▼───────┐
            │ MongoDB      │
            │   Atlas      │
            └──────────────┘
```

---

This architecture is designed to be:
- ✅ **Scalable** - Can grow with your user base
- ✅ **Maintainable** - Clear separation of concerns
- ✅ **Secure** - Multiple security layers
- ✅ **Fast** - Optimized queries and batching
- ✅ **Reliable** - Stateless design for redundancy

**Ready for production deployment!** 🚀
